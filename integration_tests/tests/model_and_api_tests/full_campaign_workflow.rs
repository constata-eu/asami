use ::api::on_chain::*;

app_test!{ full_campaign_workflow(a) 
  let mut advertiser = a.client().await; 
  let mut alice = a.client().await;
  let mut bob = a.client().await;


  // Signup an advertiser via api
}
